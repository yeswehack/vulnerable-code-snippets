version: '3.8'
services:
    php-apache:
        container_name: vsnippet-open-redirect-invalid-regex-9
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "127.0.0.1:1337:80"
